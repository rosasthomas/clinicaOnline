import { ChangeStatusColorDirective } from './change-status-color.directive';

describe('ChangeStatusColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeStatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
