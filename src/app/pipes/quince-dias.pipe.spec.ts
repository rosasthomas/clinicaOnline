import { QuinceDiasPipe } from './quince-dias.pipe';

describe('QuinceDiasPipe', () => {
  it('create an instance', () => {
    const pipe = new QuinceDiasPipe();
    expect(pipe).toBeTruthy();
  });
});
