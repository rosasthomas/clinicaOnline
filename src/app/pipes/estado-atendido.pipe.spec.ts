import { EstadoAtendidoPipe } from './estado-atendido.pipe';

describe('EstadoAtendidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoAtendidoPipe();
    expect(pipe).toBeTruthy();
  });
});
