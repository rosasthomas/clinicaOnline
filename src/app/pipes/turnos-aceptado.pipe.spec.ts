import { TurnosAceptadoPipe } from './turnos-aceptado.pipe';

describe('TurnosAceptadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosAceptadoPipe();
    expect(pipe).toBeTruthy();
  });
});
