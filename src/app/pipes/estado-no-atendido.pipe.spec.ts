import { EstadoNoAtendidoPipe } from './estado-no-atendido.pipe';

describe('EstadoNoAtendidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoNoAtendidoPipe();
    expect(pipe).toBeTruthy();
  });
});
