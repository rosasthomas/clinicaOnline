import { ListaHabilitadosPipe } from './lista-habilitados.pipe';

describe('ListaHabilitadosPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaHabilitadosPipe();
    expect(pipe).toBeTruthy();
  });
});
