import { VerResenaPipe } from './ver-resena.pipe';

describe('VerResenaPipe', () => {
  it('create an instance', () => {
    const pipe = new VerResenaPipe();
    expect(pipe).toBeTruthy();
  });
});
