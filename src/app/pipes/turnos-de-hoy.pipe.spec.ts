import { TurnosDeHoyPipe } from './turnos-de-hoy.pipe';

describe('TurnosDeHoyPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosDeHoyPipe();
    expect(pipe).toBeTruthy();
  });
});
